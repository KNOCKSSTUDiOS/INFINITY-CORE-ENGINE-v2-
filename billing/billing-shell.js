// BILLING SHELL · stub for Stripe / payments integration

window.InfinityBilling = {
  createSession(config) {
    console.log("[BILLING] Create session with config:", config);
    // Wire to Stripe or other provider later.
  },
};

