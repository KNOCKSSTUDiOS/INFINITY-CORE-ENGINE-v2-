// SPIRIT UI · bilingual hints, microcopy

window.InfinitySpirit = {
  getTagline(lang = "en") {
    if (lang === "es") {
      return "Motor de imagen de producción total, bajo tu nombre.";
    }
    return "Full production imaging engine, under your name only.";
  },
};

